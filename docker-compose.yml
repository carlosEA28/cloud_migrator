version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: cloud_migrator_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: cloud_migrator
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: cloud_migrator_redis
    restart: unless-stopped
    ports:
      - "6379:6379"

volumes:
  postgres_data:
